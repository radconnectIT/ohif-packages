
/**
 * Import namespace.
 */

import { OHIF, HangingProtocols } from  '../namespace';

/**
 * Import the scripts which will populate the namespace.
 */

import '../imports/both';

/**
 * Export the resulting objects.
 */

export { OHIF, HangingProtocols };
