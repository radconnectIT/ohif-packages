
/**
 * Import namespace.
 */

import { OHIF, Base } from  '../namespace';

/**
 * Import the scripts which will populate the namespace.
 */

import '../imports/both'
import '../imports/client'

/**
 * Export the resulting objects.
 */

export { OHIF, Base };
